# Minecraft font placeholder

The `minecraft.woff2` file included in this directory is an empty placeholder so the development build succeeds. Replace it with an authentic Minecraft font variant (for example, `MinecraftTen-VGO00.woff2`) to see the UI rendered with the correct typeface.
